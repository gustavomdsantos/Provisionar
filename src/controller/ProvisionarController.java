package controller;

import model.Query;

public class ProvisionarController {

	/**
	 * Método que calcula a correção monetária de um valor qualquer.
	 * 
	 * @param query o conjunto de dados necessários para o cálculo dos juros.
	 * @return o valor corrigido monetariamente.
	 */
	public static double calculaCorrecao (Query query) {
		return 0;
	}
}
